---
description: ローカルコミットはソース・.md/.html 等の変更前。メッセージはチャット先頭300文字。push は明示依頼時のみ。実行後は応答で必ず報告。
alwaysApply: true
---

# ソース変更前の Git ローカルコミット（チャット運用）

## 原則（このリポジトリで常に守る）

1. **ローカルコミットは、ソースコード・仕様ドキュメント（`*.md` / `*.html`）・設定・ルール等の版管理対象を変更する前に実行する**  
   そのターンで編集ツール（検索置換・パッチ・Write 等）を使う**前の最初のステップ**として `git status` し、下記「手順」に従う（作業ツリーがクリーンならコミットは省略可）。
2. **そのコミットに使う `git commit -m` のメッセージは、当該ターンのユーザーのチャットメッセージの先頭から最大 300 文字（Unicode コードポイント）とする**  
   改行・タブは半角スペースへ、trim。空なら `checkpoint: コード変更前` 等へフォールバック。
3. **エージェントがこのルールに従って `git commit` および／またはユーザー明示依頼による `git push` を実行したターンでは、応答本文で必ずユーザーへ報告する**  
   例: コミットハッシュ・ステージしたパスの要約・`git push` の成否（リモート・ブランチ名・エラー時は要約）。**サイレントで終わらせない。**

※ **記憶・ルール化の正本**は本ファイル（`.cursor/rules/pre-edit-git-commit-push.mdc`）。チャットをまたいでも Cursor が常時参照する。

## ユーザーからの明示指示（必ず守る）

ユーザーが **「編集前にローカルコミットして」** と書いたターンでは、このルールを **省略・後回しにしない**。`git status` → 可能ならローカル `git commit` → **その後**に編集ツール（検索置換・パッチ・Write 等）を使う。

**このリポジトリでソースコード・`*.md`・`*.html`・設定・ルール等を書き換えるターンでは、検索置換・パッチ・Write 等の編集ツールを使う前の最初のステップとして**、次を実行する（ユーザーが明示的に「コミットしない」と書いた場合は除く）。

- **`git push`**: **依頼のないターンでは自動実行しない**（勝手に毎回プッシュしない）。ユーザーが **プッシュ・リモート反映を明示**したターンでは、その範囲で `git push` を実行してよい。実行したら **原則 3** のとおり応答で報告する。
- **優先順位**: `git status` → 変更があればローカル `git commit` → **その後**にファイル編集へ進む →（ユーザーがプッシュを依頼したターンなら）`git push` と報告。

## コミットメッセージ（必須・この手順で実行するローカルコミット）

**ソース変更前など、このルールに従ってエージェントが実行する `git commit` のメッセージは、必ず「そのターンのユーザーがチャットに入力した文字列の先頭から最大 300 文字」とする**（以降、**チャット先頭 300 文字ルール**）。ユーザーが依頼の冒頭で意図を書けば、そのまま履歴の要約になる。

- 複数メッセージに分けて送った場合は、**いま応答しているそのターンのユーザー入力**の先頭から数える。
- サニタイズ（改行・タブ→半角スペース、trim）とフォールバックは下記「手順」のとおり。

## 手順

1. **`git status`** で作業ツリーを確認する。
2. **コミット対象があるときのみ**（変更・追加・削除が 1 件でもあるとき）:
   - **ステージの範囲**: 作業ツリーに **今回の依頼と無関係な変更が多数**ある場合、`git add -A` だと **意図しないファイルまでコミット**しうる。次のいずれかとする。
     - ユーザーに **変更一覧の要約**と **ステージする範囲**を確認する。
     - または **このチャットで編集する予定のファイル**と、同じコミットに含める必要がある付随ファイルだけを **`git add <path>...`** でステージする。
   - 上記の懸念がなく、ユーザーが全変更のスナップショットを求めている場合は **`git add -A`** でもよい（`.gitignore` で除外されるものはそのまま）。
   - コミットメッセージは **チャット先頭 300 文字ルール**に従う（**チャットに入力した文字列の先頭から最大 300 文字**＝そのターンのユーザー依頼）。
     - **文字数**: Unicode のコードポイントで数えてよい（Python なら `[:300]`）。
     - **改行・タブ**: `git commit -m` で問題にならないよう **半角スペースに置き換え**、前後の空白は trim する。
     - メッセージが空になる場合は **`checkpoint: コード変更前`** など短い日本語にフォールバックする。
   - **`git commit -m "<メッセージ>"`** を実行する（**ローカルリポジトリのみ更新**）。

## コミットを省略してよい場合

- 作業ツリーが **クリーン**（コミットする変更がない）なときは、コミットは省略し、そのまま編集に進む。
- **質問のみ・読み取りのみ**でファイルを変更しないときは、この手順は不要。

## 既存ルールとの関係

- **このルールでのローカルコミット**は、メッセージを **チャット先頭 300 文字ルール**で決める（ユーザーが日本語で書けば日本語のまま履歴に残る）。
- **ソース管理の Generate** など、**別経路で AI がメッセージを生成するコミット**は、リポジトリの **日本語コミットメッセージ**ルールに従う（チャット先頭 300 文字ルールの対象外）。
- **配台ロジックを変更したターン**では、**`.cursor/rules/dispatch-docs-sync.mdc`** に従い **`配台ルール.md` / `配台ルール.html`** を更新する（コミットの前後はそのターンの作業フローに合わせ、変更一式を同じ履歴に載せることを推奨）。
